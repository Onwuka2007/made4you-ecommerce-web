# To Start the Tailwind CLI build process
npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch

# To Run Locally
- npx serve
- install serve@14.2.5 package (By accepting)
- Choose src as the root directory